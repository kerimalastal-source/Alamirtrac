# ملف سياق الجلسة — مشروع ALAMIR TRAC

هذا الملف مُعدّ لنقل سياق العمل إلى محادثة جديدة مع Claude. عند بدء محادثة جديدة،
أرفق هذا الملف أو اطلب من Claude قراءته أولاً قبل المتابعة.

## نظرة عامة على المشروع

- **الموقع**: موقع شركة "الأمير" (ALAMIR TRAC) لصيانة وإعادة تأهيل المعدات
  الثقيلة وأنظمة الهيدروليك، في القاهرة، مصر.
- **الدومين الحي**: alamirtrac.com و www.alamirtrac.com (منشور فعلياً وقيد التشغيل).
- **المستودع**: kerimalastal-source/Alamirtrac (GitHub) — Next.js 16 (App Router,
  Turbopack)، React 19، TypeScript، Tailwind CSS v4.
- **فرع العمل**: `claude/youthful-cannon-ph9p6q` — كل التطوير يتم عليه ثم يُدفع وينشر.
- **الاستضافة**: Vercel. اسم المشروع في Vercel: `alamirtrac`،
  Project ID: `prj_Djvjft8XYytIAyPzakYydOCqlEUP`.

## دورة النشر المعتمدة (تُستخدم في كل تعديل)

1. تعديل الكود محلياً في `/home/user/Alamirtrac`.
2. `npm run build` للتأكد من عدم وجود أخطاء.
3. `git add` للملفات المحددة (تجنب `git add -A`)، ثم `git commit` مع رسالة عربية
   واضحة **ويجب أن تنتهي بسطر الإسناد الإلزامي** (Co-Authored-By + رابط الجلسة، كما
   هو محدد في تعليمات النظام وقت الجلسة).
4. `git push -u origin claude/youthful-cannon-ph9p6q`.
5. عبر أدوات Vercel MCP: `list_deployments` (فلترة بـ branch) لإيجاد الـ deployment
   الجديد → `get_deployment` للانتظار حتى `readyState: "READY"`.
6. `create_deployment` بنفس `deploymentId` و `project: "prj_Djvjft8XYytIAyPzakYydOCqlEUP"`
   و `target: "production"` → انتظار `READY` مرة أخرى (هذا يُلحق تلقائياً aliases:
   `alamirtrac.com`، `www.alamirtrac.com`، `alamirtrac-three.vercel.app`).
7. **مهم**: `alamirtrac-hadara1.vercel.app` **لا يتحدّث تلقائياً أبداً** — يجب استدعاء
   `assign_alias` صراحة بعد كل نشر إنتاجي لربطه بالـ deployment الجديد يدوياً.
8. تقرير نهائي للمستخدم بالعربية يشرح ماذا تغيّر.

ملاحظة بيئية: الشبكة الخارجية (اتصال HTTPS للإنترنت العام) محجوبة تماماً من بيئة
التنفيذ هذه — لا يمكن فتح alamirtrac.com مباشرة للتحقق، فالتحقق يتم دائماً عبر
تشغيل خادم محلي (`npm run start -- -p 3100`) وفحصه بـ curl/Playwright محلياً، قبل
النشر على الإنتاج.

## ما تم إنجازه سابقاً (خلاصة تاريخ الجلسات السابقة)

- بناء الموقع بالكامل من الصفر (كان سابقاً على Wix، تم الانتقال لـ Vercel/Next.js).
- تصميم صناعي (industrial) بلوجو، وألوان الهوية (برتقالي `#f48615` + كحلي داكن
  `#0f172a`)، وخطوط عربية (Cairo للعناوين، Tajawal للنصوص).
- 15 خدمة صيانة بصفحات تفاصيل مستقلة لكل خدمة.
- صفحة اتصل بنا مع خريطة Google Maps مضمّنة، ونموذج تواصل يرسل عبر Resend
  (`app/api/contact`).
- أزرار تواصل عائمة (اتصال، واتساب، فيسبوك، إنستغرام) — **يوجد فيها خلل مؤكد، انظر
  أدناه**.
- صفحة "من نحن"، صفحة "الأسئلة الشائعة" (مع FAQPage schema)، بحث داخل الموقع
  (SiteSearch)، 10+ مقالات مدونة تقنية.
- Google Analytics (`G-8N9W3E758L`) و Google Search Console مفعّلان.
- **تدقيق SEO احترافي كامل (منجز وتم نشره فعلياً على الإنتاج)**:
  - أُصلح غياب H1 في 5 صفحات (الخدمات، المشاريع، المدونة، الأسئلة الشائعة، اتصل بنا)
    عبر إضافة خاصية `as` لمكوّن `components/SectionHeading.tsx`.
  - أُصلحت 5 أوصاف meta description كانت أطول من اللازم (الرئيسية + 4 خدمات) عبر
    حقل `metaDescription` اختياري جديد في `lib/content.ts`.
  - تم توحيد `areaServed` في البيانات المنظمة (schema.org) ليكون "مصر" (Country)
    بدلاً من "القاهرة" فقط، في `lib/schema.ts`.
  - أُضيفت `alternateName` بصيغ العلامة التجارية المختلفة (الأمير تراك / الامير تراك
    / Alamir Trac / ALAMIR TRAC) في LocalBusiness schema، وأُضيف WebSite schema
    جديد بنفس الأسماء البديلة، لتقوية ظهور الموقع عند البحث بأي من هذه الصيغ.
  - تم التحقق ببرنامج فحص Python مخصص (`/tmp/claude-0/seo-audit.py` — ملف مؤقت غير
    موجود في المستودع) يفحص كل الـ36 رابط في sitemap.xml: صفر مشاكل بعد الإصلاح
    (لا H1 مفقود/مكرر، لا عناوين/أوصاف مكررة، JSON-LD وOG سليمة في كل صفحة).
  - **هذا العمل تم دفعه ونشره بالكامل على الإنتاج بنجاح.**

## المهمة الحالية (قيد التنفيذ — لم تكتمل بعد، لا يوجد أي تعديل كود لها بعد)

**طلب المستخدم الأخير حرفياً**: يريد رأي خبير تصميم/هندسة مواقع احترافي حول الموقع
من الناحية التقنية والهندسية والتصميمية والمرئية — هل الموقع "جميل"؟ وإن وُجدت أخطاء
يريد إصلاحها تقنياً، بصفتي خبيراً في تصميم المواقع "الفخمة والفاخرة"، بما يتناسب
مع نشاط الشركة (صيانة معدات ثقيلة) وخدماتها.

### الخطوات المنفذة حتى الآن في هذه المهمة

1. قرأتُ كل الملفات الأساسية للتصميم: `app/globals.css`، `components/Header.tsx`،
   `Footer.tsx`، `HeroIllustration.tsx`، `ServiceCard.tsx`،
   `FloatingContactButtons.tsx`، `CoverIllustration.tsx`، `ServiceHero.tsx`،
   `ContactForm.tsx`، و`app/about/page.tsx`، `app/projects/page.tsx`،
   `app/contact/page.tsx`.
2. شغّلت خادم إنتاج محلي على المنفذ 3100 (`npm run start -- -p 3100`).
3. التقطت لقطات شاشة كاملة (Playwright + Chromium من `/opt/pw-browsers/chromium`،
   مع `NODE_PATH=/opt/node22/lib/node_modules` لأن Playwright مثبّت عالمياً وليس في
   `node_modules` الخاص بالمشروع) لصفحات: الرئيسية، من نحن، الخدمات، تفاصيل خدمة
   (rehabilitation)، أعمالنا، المدونة، الأسئلة الشائعة، اتصل بنا — على مقاسين:
   Desktop (1440×900) وMobile (390×844)، محفوظة في `/tmp/claude-0/shots/`
   (مجلد مؤقت، غير موجود في المستودع، سيُفقد عند انتهاء هذه الجلسة).
4. راجعتُ بصرياً حتى الآن: `home-desktop.png`، `home-mobile.png`،
   `about-desktop.png`، `services-desktop.png`.

### النتائج المؤكدة حتى الآن

**خلل حقيقي مؤكد (Bug حقيقي وليس وهم لقطة الشاشة)**: أزرار التواصل العائمة
(`components/FloatingContactButtons.tsx`) — اتصال، واتساب، فيسبوك، إنستغرام —
مكدّسة عمودياً بـ `fixed bottom-5 start-5` بارتفاع إجمالي حوالي 228px (4 أزرار
48px + فراغات 12px). على شاشات الموبايل (تحققتُ فعلياً بلقطات شاشة عند تمرير
0px و600px على viewport بعرض 390): هذا الكومة من الأزرار **تُغطي فعلياً** زر
الدعوة الرئيسي "اطلب صيانة الآن" في قسم الهيرو، وتُغطي جزئياً عنوان قسم
"حلول صيانة شاملة" عند التمرير. تم التحقق أن هذا ليس مجرد قصور في التقاط لقطة
الصفحة الكاملة (fullPage screenshot) بل سلوك حقيقي في viewport فعلي — لقطتا
`scroll-0.png` و `scroll-600.png` في `/tmp/claude-0/shots/` تثبتان ذلك بوضوح.

**الحل المخطط له (لم يُنفذ بعد في الكود)**: تقليص الأزرار العائمة على الموبايل إلى
زرّي الاتصال والواتساب فقط (الأهم تحويلياً)، مع إبقاء فيسبوك وإنستغرام ظاهرين فقط
من مقاس `sm:` فأكبر، أو إعادة ترتيب/تصغير الحجم لتفادي التداخل. يحتاج تعديل على
`components/FloatingContactButtons.tsx` بإضافة classes متجاوبة (`hidden sm:flex`
على الأزرار الأقل أهمية على الموبايل تحديداً).

**ملاحظات تصميمية أولية (من المراجعة البصرية لصفحات Desktop: الرئيسية، من نحن،
الخدمات)**:
- التصميم العام نظيف واحترافي (نمط "شركي" corporate واضح، تسلسل بصري جيد، توافق
  ممتاز مع RTL)، لكنه **أقرب إلى تصميم SaaS عام مسطّح (flat) منه إلى هوية "فخمة/
  فاخرة"** كما طلب المستخدم تحديداً.
- **لا توجد أي صور فوتوغرافية حقيقية في الموقع** — الملف الوحيد في `public/` هو
  `logo.avif`. كل الرسومات هي أيقونات خطية SVG بسيطة (دوائر مدارية في الهيرو،
  أيقونات مجردة في `CoverIllustration`/`ServiceHero`). لشركة صيانة معدات ثقيلة،
  صور حقيقية للورشة/المعدات/الفريق ترفع الإحساس بالفخامة والمصداقية بشكل كبير —
  لكن **لا توجد أصول صور متاحة حالياً** في المشروع لاستخدامها؛ هذه نقطة يجب
  توضيحها للمستخدم (قد يحتاج تزويدنا بصور حقيقية من الورشة والمعدات).
- قسم "لماذا ALAMIR TRAC" في الصفحة الرئيسية (4 بطاقات قيمة) **بلا أيقونات إطلاقاً**
  — مربعات نصية فقط بحدود، بينما القسم الذي قبله (بطاقات الخدمات) يستخدم أيقونات
  ملونة بارزة. هذا **تضارب في الإيقاع البصري** بين الأقسام، يستحق توحيد التصميم
  بإضافة أيقونات مماثلة لقسم "لماذا نحن" ليتماشى مع بقية الصفحة.
- تسلسل الهيدر/الهيرو/الإحصائيات/بطاقات الخدمات/المدونة/شريط الدعوة البرتقالي/
  الفوتر — كلها تُعرض بشكل نظيف من ناحية المسافات والمحاذاة في RTL.

### لم تتم مراجعته بعد (خطوات متبقية صريحة)

- لقطات الموبايل لصفحات: من نحن، الخدمات، تفاصيل خدمة، أعمالنا، المدونة، الأسئلة
  الشائعة، اتصل بنا (كلها ملتقطة فعلياً في `/tmp/claude-0/shots/*-mobile.png` لكن
  لم تُقرأ/تُراجع بصرياً بعد).
- لقطات Desktop لصفحات: تفاصيل خدمة (`service-detail-desktop.png`)، أعمالنا
  (`projects-desktop.png`)، المدونة (`blog-desktop.png`)، الأسئلة الشائعة
  (`faq-desktop.png`)، اتصل بنا (`contact-desktop.png`) — ملتقطة لكن غير مُراجعة.
- **لم يتم تنفيذ أي إصلاح كود فعلي بعد لهذه المهمة** — لا commit، لا push، لا نشر.
  كل ما سبق هو مرحلة تدقيق/مراجعة فقط.

### الخطوات التالية المخطط لها

1. إكمال مراجعة كل لقطات الشاشة المتبقية (الموبايل بالكامل + Desktop المتبقي)
   لحصر كل الأخطاء التقنية/البصرية الفعلية (وليس افتراضات).
2. تجميع قائمة نهائية بكل الأخطاء المؤكدة (بصرية، تقنية، UX) مع أولوياتها.
3. تنفيذ الإصلاحات كوداً، أهمها:
   - إصلاح تداخل الأزرار العائمة مع المحتوى على الموبايل (مؤكد وعاجل).
   - توحيد استخدام الأيقونات في قسم "لماذا نحن" بالصفحة الرئيسية.
   - أي أخطاء إضافية تظهر من مراجعة باقي اللقطات (تباين ألوان، تراكب عناصر،
     تباعد غير متسق، إلخ).
   - تحسينات اختيارية لرفع الإحساس بالفخامة ضمن حدود الأصول المتاحة (بدون صور
     حقيقية): تدرجات لونية أدق، ظلال/عمق أكثر احترافية، حركات/تفاعلات ناعمة عند
     التمرير (hover/transition)، تحسين التناسق بين الأقسام.
4. إعادة البناء (`npm run build`) والتحقق محلياً (لقطات شاشة جديدة + إعادة تشغيل
   `seo-audit.py` للتأكد من عدم كسر أي شيء في SEO أثناء تعديلات التصميم).
5. Commit (مع سطر الإسناد الإلزامي) → Push → دورة نشر Vercel الكاملة (بما فيها
   `assign_alias` اليدوي لـ `alamirtrac-hadara1.vercel.app`).
6. تقرير نهائي بالعربية للمستخدم: ما هي الأخطاء التي وُجدت، كيف أُصلحت، وأي قيود
   صادقة (مثل الحاجة لصور حقيقية لرفع مستوى الفخامة بشكل أكبر مستقبلاً).

## ملفات مهمة للرجوع إليها بسرعة

- نظام التصميم/الألوان: `app/globals.css`
- المكوّنات المشتركة: `components/Header.tsx`, `Footer.tsx`,
  `FloatingContactButtons.tsx` (فيه الخلل المؤكد), `HeroIllustration.tsx`,
  `CoverIllustration.tsx`, `ServiceHero.tsx`, `ServiceCard.tsx`,
  `SectionHeading.tsx`, `ContactForm.tsx`, `SiteSearch.tsx`
- المحتوى/البيانات: `lib/content.ts` (كل بيانات الشركة والخدمات والمشاريع
  والمدونة), `lib/schema.ts` (JSON-LD schema.org), `lib/search.ts`
- الصفحات: `app/page.tsx`, `app/about/page.tsx`, `app/services/page.tsx`,
  `app/services/[slug]/page.tsx`, `app/projects/page.tsx`, `app/blog/page.tsx`,
  `app/blog/[slug]/page.tsx`, `app/faq/page.tsx`, `app/contact/page.tsx`,
  `app/layout.tsx`
